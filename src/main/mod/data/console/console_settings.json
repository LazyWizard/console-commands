{
    # The path to the bitmap font used by the overlay
    "consoleFont":"graphics/fonts/scp_16.fnt", # The default console font (monospaced)
    #"consoleFont":"graphics/fonts/insignia15LTaa.fnt", # Starsector's font (not monospaced)

    # Valid levels: OFF, FATAL, ERROR, WARN, INFO, DEBUG, ALL
    # INFO and above will save all console output to Starsector.log!
    "consoleLogLevel":INFO            # Default: INFO
}
